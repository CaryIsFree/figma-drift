{
  "name": "@figma-drift/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "figma-drift": "./dist/cli.js"
  },
  "scripts": {
    "dev": "npx tsx watch src/cli.ts",
    "build": "tsc --outDir dist",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@figma-drift/backend": "workspace:*",
    "commander": "^12.0.0",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.30"
  }
}
